export class Seasons {

    public season: number;
    public episode: number;
    public title: string;

    constructor(season: number, episode: number, title: string) {
      this.season = season;
      this.episode = episode;
      this.title = title;
    }
  }
